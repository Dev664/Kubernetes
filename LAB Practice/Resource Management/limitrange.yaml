apiVersion: v1
kind: LimitRange

metadata:
  name: resource-limits

spec:
  limits:
  - type: Container

    min:
      cpu: 100m
      memory: 64Mi

    max:
      cpu: 1
      memory: 1Gi

    defaultRequest:
      cpu: 200m
      memory: 128Mi

    default:
      cpu: 500m
      memory: 256Mi
