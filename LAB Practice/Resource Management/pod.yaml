apiVersion: v1
kind: Pod

metadata:
  name: quota-test-1

spec:
  containers:
  - name: nginx
    image: nginx:1.27

    resources:
      requests:
        cpu: 500m
        memory: 600Mi

      limits:
        cpu: 1
        memory: 1Gi
